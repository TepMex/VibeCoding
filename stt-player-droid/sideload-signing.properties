# Committed sideload keystore (stt-player-droid/sideload.keystore). Same signing key on every clone/CI build
# so APK updates install over the previous app. Not for Play distribution.
storeFile=sideload.keystore
storePassword=vibecoding
keyAlias=vibecoding
keyPassword=vibecoding
